export const metadata = {
  title: "Verify Email",
  description: "Verify your email address for ProofPopify.",
};

export default function VerifyEmailLayout({ children }) {
  return <>{children}</>;
}
