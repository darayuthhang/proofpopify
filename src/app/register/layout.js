export const metadata = {
  title: "Create Account",
  description: "Create your free ProofPopify account and boost your sales.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}
