export const metadata = {
  title: "Upgrade Plan",
  description: "Upgrade your ProofPopify plan to get more features.",
};

export default function UpgradeLayout({ children }) {
  return <>{children}</>;
}
