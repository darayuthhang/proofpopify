export const metadata = {
  title: "Dashboard",
  description: "Manage your startups and Stripe notification widgets.",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
