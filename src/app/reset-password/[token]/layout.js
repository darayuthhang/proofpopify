export const metadata = {
  title: "Reset Password",
  description: "Set a new password for your ProofPopify account.",
};

export default function ResetPasswordLayout({ children }) {
  return <>{children}</>;
}
